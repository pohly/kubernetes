---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: classparameters.dra.e2e.example.com
spec:
  group: dra.e2e.example.com
  names:
    kind: ClassParameter
    listKind: ClassParameterList
    plural: classparameters
    singular: classparameter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClassParameter is the type used for ResourceClasss.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: "ClassParameterSpec holds the custom values of a ClassParameter.
              \n In contrast to ClassParameterSpec, only setup parameters can be specified."
            properties:
              env:
                additionalProperties:
                  type: string
                description: Env are environment variables that get injected into
                  the container sandbox by the driver.
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
